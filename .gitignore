node_modules/
dist/
*.log
.DS_Store
.env
.env.local

# local testing — runner scaffolding is committed, but secrets and runtime
# artifacts (state, logs) stay out of git.
local-test/.env
local-test/workspace/
local-test/*.log
local-test/cron.log

# production runtime artifact (created by run-cron.bat on the Windows server)
cron.log
